name: qtcloud_write_studio
description: AI-native writing studio for qtcloud-write(写作云 AI 原生写作编辑器).
publish_to: none
version: 0.1.0-alpha.4

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  equatable: ^2.0.5
  path: ^1.8.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
